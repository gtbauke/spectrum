[project]
name = "db-core"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "alembic>=1.18.3",
    "asyncpg>=0.31.0",
    "sqlalchemy>=2.0.46",
    "core",
]

[tool.uv.sources]
core = { workspace = true }

[tool.setuptools]
packages = ["db"]
