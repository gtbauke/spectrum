[project]
name = "workers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "eggp>=1.0.16",
    "pandas>=3.0.0",
    "pyarrow>=19.0.1",
    "aio-pika>=9.5.4",
    "aioboto3>=13.4.0",
    "core",
    "db-core",
]

[tool.uv.sources]
core = { workspace = true }
db-core = { workspace = true }

[tool.setuptools]
packages = ["adapters", "handlers", "services", "utils"]
